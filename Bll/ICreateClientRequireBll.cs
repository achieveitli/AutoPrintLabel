using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using Entity;

namespace Bll
{
   public interface ICreateClientRequireBll
   {
       void CreateClientRequire(t_labels label);
        void CreateClientRequireOne(t_labels label);
        void CreateClientRequireTwo(t_labels label);
        void CreateClientRequireThree(t_labels label);
        void CreateClientRequireFour(t_labels label);
        void CreateClientRequireFive(t_labels label);
   }
}
