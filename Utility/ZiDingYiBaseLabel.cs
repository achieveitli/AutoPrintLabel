using Entity;
using System;
using System.Collections.Generic;
using System.Linq;
using System.Reflection;
using System.Text;

namespace Utility
{
    public class ZiDingYiBaseLabel
    {

        //public Object Convertlabel(BaseLabel baseLabel,Object obj)
        //{
        //    PropertyInfo[] pro_base = baseLabel.GetType().GetProperties();
        //    PropertyInfo[] pro_obj = obj.GetType().GetProperties();
        //    foreach (var objs in pro_obj)
        //    {
        //        foreach (var bases in pro_base)
        //        {
        //            //获取字段名
        //            string name = item.Name;
        //            sb.Append(name + ":{" + i + "},");
        //            i++;
        //            sb2.Append(name.ToString() + ",");
        //        }
        //    }
        //}
        
    }
}
